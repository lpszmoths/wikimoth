# Wikimoth

A repository of snippets

## Installation

```
sudo apt-get install -y python3 pip pkg-config libcairo2-dev libjpeg62-turbo-dev libpango1.0-dev libgif-dev build-essential g++
git clone git@github.com:lpszmoths/wikimoth.git
cd wikimoth
python3 -m pip install -r requirements.txt
```

